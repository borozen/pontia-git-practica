on:
  push:
    branches:
    - main
inputs:
  envirnment:
    description: "Un texto cualquiera"
    required:  true
    type: text
jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
      - name: Imprimir mensaje
      run:
